{
  "rules": {
    ".read": false,
    ".write": false,
    "portfolioProjectImages": {
      "$projectId": {
        ".read": true,
        "$imageId": {
          ".write": "newData.val() == null || (newData.hasChildren(['order', 'value']) && newData.childrenCount == 2 && newData.child('order').isNumber() && newData.child('order').val() >= 0 && newData.child('order').val() < 24 && newData.child('value').isString() && newData.child('value').val().length > 0 && newData.child('value').val().length <= 10485760)"
        }
      }
    }
  }
}